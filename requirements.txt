# FastAPI Backend - PulmoMed
# Version: 2.2 - PostgreSQL pgvector + FastEmbed (sin PyTorch)

# API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.2.0

# Vector search in PostgreSQL (reemplaza ChromaDB)
pgvector==0.3.6

# Embeddings ONNX - sin transformers/torch (~90MB vs ~2GB)
fastembed==0.5.1

# PDF Processing (para indexación de documentos médicos)
pypdf==4.0.1
python-multipart==0.0.6

# HTTP Client (para Ollama / Groq)
httpx==0.26.0

# Database (PostgreSQL async)
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Numeric solver (Gompertz + RK4)
numpy==1.26.4

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Utilities
python-dotenv==1.0.0

# Linting
flake8
